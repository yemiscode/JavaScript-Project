// the pop method removes the last element from an array and then it can also return that element that was remove
const fruits = ['mango', 'orange', 'banana', 'apple'];
const popped = fruits.pop();
console.log(fruits);
console.log(popped);

// answer
// (3) ['mango', 'orange', 'banana']
//  apple