// checks if a given argument is an array or not  (it is a static method)
const lang = 'javascript';
console.log(Array.isArray(lang));

let numbers = [1, 3, 9, 0, 23, 4];
console.log(Array.isArray(numbers))

// answer
// false
//  true